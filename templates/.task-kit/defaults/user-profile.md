# ユーザープロファイル(既定値)

## new-task 既定値
- 既定作成者:
- 既定担当者:

## 運用メモ
- `/task-kit.new-task` で作成者が確定したとき、このファイルの「既定作成者」が空欄なら初回値として保存する。次回以降は既定値として再利用する。
- `/task-kit.new-task` で担当者が確定したとき、このファイルの「既定担当者」が空欄なら初回値として保存する。次回以降は既定値として再利用する。
- 依頼者の未指定時は、担当者と同じ値を使う。
